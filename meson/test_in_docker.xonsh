#!/usr/bin/env xonsh

#docker container run --name foam -di alpine

script="""
set -e

apk add git python3

cd root
git clone https://github.com/Volker-Weissmann/meson --depth 1
/root/meson/meson.py
"""

docker exec -it foam sh -c @(script)

#docker rm @(container[:12])
